using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestClass : Core {
    // Use this for initialization
    private void Awake()
    {
        BindingController.Init();
    }

    // Update is called once per frame

}
